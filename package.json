{
  "name": "mysterious-crawler",
  "version": "1.0.0",
  "description": "SR",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc --project tsconfig.build.json",
    "dev": "tsc --project tsconfig.build.json && node dist/index.js",
    "test": "vitest run --coverage",
    "test:rof": "vitest run --coverage --coverage.reportOnFailure",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "author": "SR",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^1.3.1",
    "prettier": "^3.6.2",
    "typescript": "^5.4.2",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
